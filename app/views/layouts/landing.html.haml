!!!
%html
  %head
    %meta{name: "viewport", content: "width=device-width, initial-scale=1, maximum-scale=1"}
    %meta{name: "google-site-verification", content: "E0UG-Of2okcNlQj9QZ0HTT8rs_Oqwip1Y3bvL4zsyJ0"}
    %title= content_for?(:title) ? yield(:title) : "LensHQ"
    %meta{:content => content_for?(:description) ? yield(:description) : t('.lens_description'), name: "description"}
    = stylesheet_link_tag :application, :media => "all"
    = csrf_meta_tags
    = yield(:head)
  %body
    #main.landing{:role => "main"}
      = yield
    %footer
    = javascript_include_tag :application
    = render 'ga'
