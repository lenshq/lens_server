.panel.panel-default
  .panel-heading
    %h2= t('.applications')
  .panel-body
    - if @applications.any?
      = render 'applications'
    - else
      .alert.alert-dismissable.alert-info
        = t('.no_applications_yet')
    = link_to t('.new'), new_application_path, class: 'btn btn-primary'
